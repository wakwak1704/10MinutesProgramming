const HEIGHT = 19;
const WIDTH = 10;

$(function() {

    let fixedBlocks = new Arra(HEIGHT * WIDTH).fill(false);

});

function generatePiece() {
    
    for(let i = 0; i < )
    let p = Math.random(1);
}
function (obj) {
    if(obj.id == 'start-btn') {
        $('#start-btn').css('display', 'none');
        $('#reset-btn').css('display', 'block');
    } else if(obj.id = 'reset-btn') {
        $('#start-btn').css('display', 'block');
        $('#reset-btn').css('display', 'none');
    }
}